name: Deploy to X-Server

on:
  push:
    branches:
      - main  # mainブランチにプッシュされたときに実行
  workflow_dispatch:  # 手動実行も可能

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get install -y lftp

      - name: Check FTP directory structure
        env:
          FTP_HOST: ${{ secrets.XSERVER_FTP_HOST }}
          FTP_USER: ${{ secrets.XSERVER_FTP_USER }}
          FTP_PASS: ${{ secrets.XSERVER_FTP_PASSWORD }}
        run: |
          lftp -c "
            set ssl:verify-certificate no;
            set ftp:ssl-allow yes;
            set ftp:ssl-protect-data yes;
            set ftp:initial-prot '';
            open -u $FTP_USER,$FTP_PASS $FTP_HOST;
            echo '=== Current directory ===';
            pwd;
            echo '=== Root listing ===';
            ls;
            echo '=== Trying fromround.com ===';
            ls fromround.com/ || echo 'fromround.com not found';
            echo '=== Trying public_html ===';
            ls public_html/ || echo 'public_html not found';
            quit;
          "

